import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

// Find the DOM element root and render the App inside of it
ReactDOM.render(<App />, document.getElementById("root"));
